# FoxESS
FOXESS_MODE=mock            # mock | proxy
FOXESS_PROXY_URL=           # e.g. https://your-foxess-proxy.example.com
FOXESS_DEVICE_SN=603T253021ND064

# RCE (hourly prices)
# If running locally, expose your site URL to server (for lib/providers/rce.ts). On Vercel this is set automatically.
NEXT_PUBLIC_BASE_URL=http://localhost:3000
